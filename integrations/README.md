# Enterprise Integrations

The capstone uses a mock ticket-system adapter. In a real customer environment, replace it with adapters for ServiceNow, Zendesk, Salesforce Service Cloud, Jira Service Management, or custom APIs.

```text
Business Logic → Integration Interface → Customer Adapter → Enterprise System
```

Avoid coupling business logic directly to one customer system.
