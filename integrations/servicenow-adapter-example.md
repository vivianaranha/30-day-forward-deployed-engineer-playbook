# ServiceNow Adapter Example

In production: authenticate with approved enterprise identity, map fields explicitly, validate permissions, apply idempotent updates, log the action, and return explicit success/failure. Do not let an LLM construct unrestricted API calls.
